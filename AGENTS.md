# AGENTS.md

## Repository expectations

- Follow existing code style and conventions.
- Document commands in `README.md` when you change behavior or add new commands.
- Write integration tests for new features that interact with the database.
- Always run `make format` and `make test` after modifying Go files.
